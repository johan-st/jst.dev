package web

import (
"jst.dev/internal/repo"
)


templ Blog(data DataBase, featuredPosts []repo.BlogPost) {
@Base(data)
<p>Welcome to the blog!</p>
<p>Here are some of our featured posts:</p>
@list(data, featuredPosts)
}

templ list(data DataBase, posts []repo.BlogPost) {
<ul>
    for _, post := range posts {
    @listItem(post)
    }
</ul>
}

templ listItem(post repo.BlogPost) {
<li><a href={ templ.SafeURL("/blog/" + post.Slug) }>
<h4>{ post.Title }</h4>
<div> { post.Author.Name } <span>{ post.Date.Format("2006-01-02") }</span></div>
<p>{ post.Short }</p>
</a></li>
}

templ BlogPost(data DataBase, post repo.BlogPost) {
@Base(data)
<p>{ post.Date.Format("2006-01-02") }</p>
<p>{ post.Author.Name }</p>
<div class="content">
    @ComponentMarkdown(post.Body)
</div>
}


